name: Daily Commit

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'  # Every day at midnight UTC

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set Git user
      run: |
        git config --global user.name "SriramBharath-7"
        git config --global user.email "41898282+SriramBharath-7@users.noreply.github.com"

    - name: Create or update activity log
      run: |
        echo "✅ Pinged on $(date)" >> activity.log

    - name: Commit changes
      run: |
        git add activity.log
        git commit -m "📅 Daily activity ping" || echo "No changes to commit"
        git push
